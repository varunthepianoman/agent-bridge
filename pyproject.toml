[project]
name = "agent-bridge"
version = "0.1.0"
description = "A provider-neutral authenticated mailbox for cooperating agents on Windows and Ubuntu"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
